name: freshness

on:
  schedule:
    - cron: "8 */1 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: freshness
  cancel-in-progress: false

jobs:
  freshness:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: refresh
        run: |
          set -eu
          sleep $(( RANDOM % 900 ))
          f="README.md"
          [ -f "$f" ] || exit 0
          stamp="<!-- refreshed: $(date -u +%Y-%m-%d) -->"
          if grep -q "^<!-- refreshed: " "$f"; then
            sed -i "s|^<!-- refreshed: .*|$stamp|" "$f"
          else
            printf '\n%s\n' "$stamp" >> "$f"
          fi

      - name: commit
        run: |
          set -eu
          git config user.name "dave-mio1905w9"
          git config user.email "279208033+dave-mio1905w9@users.noreply.github.com"
          set -- "docs: keep current" "docs: refresh timestamp" "docs: update generated marker" "docs: bump date" "chore: refresh metadata"
          n=$(( ${GITHUB_RUN_NUMBER:-0} % $# + 1 ))
          eval msg=\${$n}
          git add -- "README.md"
          if git diff --cached --quiet; then
            echo "nothing to commit"
            exit 0
          fi
          git commit -m "$msg"
          git push
